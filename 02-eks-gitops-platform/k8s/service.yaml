apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: orders
spec:
  # ClusterIP: internal only. The Ingress/ALB is the public entry point.
  type: ClusterIP
  selector:
    app: orders-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
